# import functions from the simpleWiresAction file so we can call them!
from simpleWiresAction import *


def simpleWiresDialog():
    # look back on the simpleWiresAction function...what information do we need to prompt for?


    # after obtaining all necessary information, call the action method and save the result
    

    # print a statement returning which wire to cut!
    return "Fill this in when actually writing the method!"